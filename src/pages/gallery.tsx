import Template from 'components/common/Template';
import { graphql } from 'gatsby';
import React from 'react';

const gallery = () => {
  return <Template>갤러리</Template>;
};

export default gallery;
